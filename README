基础配置：

1.Python Format
sudo apt install autopep8

2.CPP Format
sudo apt install clang-format

Note: YCM requires additional compilation.

...to be modified.

vundle使用方式需要更新

vundle使用方式需要更新

vundle使用方式需要更新
